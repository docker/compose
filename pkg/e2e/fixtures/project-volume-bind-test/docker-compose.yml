services:
  frontend:
    image: nginx
    container_name: frontend
    volumes:
      - project-data:/data
    security_opt:
      - label=disable

volumes:
  project-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: "${TEST_DIR}"
