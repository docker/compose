services:
  sample:
    image: alpine
    command: sh -c 'sleep 30'
    volumes:
      - data:/shared
    pre_start:
      - image: alpine
        command: sh -c 'echo v1 >> /shared/versions.txt'
volumes:
  data:
