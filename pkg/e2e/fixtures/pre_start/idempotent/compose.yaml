services:
  sample:
    image: alpine
    command: sh -c 'sleep 30'
    volumes:
      - data:/shared
    pre_start:
      - image: alpine
        command: sh -c 'echo $(cat /proc/sys/kernel/random/uuid) >> /shared/tokens.txt'
volumes:
  data:
