services:
  test:
    image: alpine
    command: sh -c "cat /run/secrets/secrets_db_password"
    depends_on:
      - secrets
  secrets:
    provider:
      type: example-provider
      options:
        name: secrets
        type: test1
        size: 1
