services:
  sample:
    image: alpine
    command: sh -c 'sleep 30'
    volumes:
      - data:/shared
    pre_start:
      - image: alpine
        command: sh -c 'echo A >> /shared/out'
      - image: alpine
        command: sh -c 'echo B >> /shared/out'
volumes:
  data:
