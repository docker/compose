services:
  incomplete:
    volumes:
      - data:/data
    networks:
      - internal
    models:
      llm:
  gated:
    profiles: ["extra"]
volumes:
  data:
networks:
  internal:
models:
  llm:
    model: ai/example
