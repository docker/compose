services:
  test:
    image: alpine
    command: env
    environment:
      CLOUD_REGION: user-defined-region
    depends_on:
      - secrets
  secrets:
    provider:
      type: example-provider
      options:
        name: secrets
        type: test1
        size: 1
