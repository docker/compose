services:
  nginx:
    build: nginx-build
    ports:
      - 8070:80

  nginx2:
    build: nginx-build
    image: gtardif/custom-nginx:1.0

  mongo:
    image: mongo

  sentences:
    image: gtardif/sentences-web