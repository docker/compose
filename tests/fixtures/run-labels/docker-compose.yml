version: "3.5"
services:
  from_image:
    image: busybox:1.31.0-uclibc
    command: top
    labels:
      foo: bar
      hello: world

  from_build:
    build:
      context: .
      labels:
        some_label: I am a label
